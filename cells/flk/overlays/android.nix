final: prev: {
  # waydroid = prev.waydroid.overrideAttrs (_: {
  #   inherit (final.fog.waydroid) src version;
  #   patches = prev.lib.flk.getPatchFiles ../pkgs/os-specific/linux/waydroid;
  # });
}
